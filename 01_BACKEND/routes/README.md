# routes/

Reserved for future route modules if `app.py` is split up (e.g. Flask
Blueprints). Empty as of this restructure — see the "Why routes/ and
services/ are currently empty" section in `01_BACKEND/README.md`.
