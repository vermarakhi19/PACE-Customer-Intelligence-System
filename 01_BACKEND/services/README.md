# services/

Reserved for future service/helper modules (e.g. moving the CSV/JSON-reading
helpers out of `app.py`). Empty as of this restructure — see the "Why
routes/ and services/ are currently empty" section in `01_BACKEND/README.md`.
